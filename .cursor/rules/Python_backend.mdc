---
description: NodyNote_Cursor專案通用協作規則
alwaysApply: true
---
# NodyNote_Cursor 專案

- 回覆使用繁體中文，不要用簡體中文(除非使用者要求使用英文)
- 不要commit / push，除非使用者明確要求
- 不要提交`.env` 或金鑰
- 優先讀 `README.md`、`main.py`、`spec.md` 再改碼
- 只改與任務相關的檔案，避免無關大重構
- 專案開發只會一個一個功能下去做開發，不會有整包開發的情況發生
- 每次動作請主動說明這一步驟做了甚麼
- 以現有架構下去開發，不需要擴，如果你覺得要擴，需先跟我說。(本專案規模小，無須以production規模開發)
- 格式不需要都空一行
- 不要覆蓋我寫的東西，除非有問題，如有問題先跟我確認